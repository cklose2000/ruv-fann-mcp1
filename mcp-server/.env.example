# ruv-FANN Enhanced MCP Server Configuration

# ruv-FANN Service URLs
# These are the backend ruv-FANN services that provide AI capabilities
RUV_FANN_CORE_URL=http://127.0.0.1:8090
RUV_FANN_SWARM_URL=http://127.0.0.1:8081
RUV_FANN_MODEL_URL=http://127.0.0.1:8082

# GCP MCP Backend Configuration
# This connects to the gcp-fresh-mcp server that proxies actual GCP operations
GCP_MCP_BACKEND_URL=http://127.0.0.1:8080
GCP_MCP_SECRET=change-this-secret-token

# Optional: Database Configuration
# SQLite databases are created automatically in the data/ directory
# You can override the paths if needed
# PATTERN_DB_PATH=./data/patterns.db
# GCP_PATTERN_DB_PATH=./data/gcp_patterns.db

# Optional: Logging Configuration
# LOG_LEVEL=info
# LOG_FORMAT=json

# Optional: MCP Server Configuration
# MCP_SERVER_NAME=ruv-fann-enhanced-mcp
# MCP_SERVER_VERSION=1.0.0

# Optional: Prediction Thresholds
# Operations with success probability below this will show warnings
# PREDICTION_WARNING_THRESHOLD=0.5
# Operations with success probability below this will be blocked
# PREDICTION_BLOCK_THRESHOLD=0.3

# Optional: Pattern Learning Configuration
# Maximum patterns to store per tool
# MAX_PATTERNS_PER_TOOL=1000
# Pattern retention days
# PATTERN_RETENTION_DAYS=30

# Optional: GCP-specific Configuration
# Default timeout for GCP operations (milliseconds)
# GCP_OPERATION_TIMEOUT=30000
# Enable cost estimation features
# ENABLE_COST_ESTIMATION=true
# Enable auth token age monitoring
# ENABLE_AUTH_MONITORING=true